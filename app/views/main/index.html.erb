<div class="d-flex align-items-center justify-content-center">
<h1>Welcomer to Scheduled Tweets</h1>
</div>

<% if Current.user %>
   Logged in as <%= Current.email %>

   <%= button_to "Logout", logout_path, method: :delete %>
 <% end %>